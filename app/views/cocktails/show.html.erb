<h1><%= @cocktail.name %></h1>

<% @doses.each do |d| %>
  <p><%= d.ingredient.name %> - <%= d.description %></p>
  <%= link_to "delete", dose_path(d), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
<% end %>


<%= link_to "Back", root_path %> <span>|</span> <%= link_to "Add ingredient", new_cocktail_dose_path(@cocktail) %>
